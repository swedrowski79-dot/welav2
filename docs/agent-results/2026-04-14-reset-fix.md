# Task

Fix the admin pipeline reset logic so `Full Reset` also clears the relevant `raw_*` tables, while `Reset Stage` continues to clear only `stage_*` tables.

# Files read

- `AGENTS.md`
- `.github/copilot-instructions.md`
- `PROJECT_CONTEXT.md`
- `agents/codex/backend-task.md`
- `docs/agent-results/2026-04-14-admin-pipeline.md`
- `src/Web/Repository/PipelineAdminRepository.php`

# Changed files

- `src/Web/Repository/PipelineAdminRepository.php`

# Summary

- Extended `fullReset()` so it now clears:
  - `raw_*` tables
  - `stage_*` tables
  - `export_queue`
  - `product_export_state`
- Kept `resetStageTables()` unchanged, so `Reset Stage` still only truncates `stage_*` tables.
- Kept the existing reset structure, confirmation dialog flow, warning texts, and reset logging.
- Extended the `full_reset` log context so the cleared `raw_*` tables are included.

# Open points

- `Full Reset` depends on `raw_*` tables being present in the admin whitelist config, which is currently true in this repository.
- No browser-level validation was run, so the change is validated at code level only.

# Validation steps

- Executed:
  - `docker compose exec php php -l /app/src/Web/Repository/PipelineAdminRepository.php`
- Not executed:
  - manual reset action via browser
  - end-to-end database reset verification

# Recommended next step

Open `/pipeline` in the admin UI and run `Full Reset` once against a disposable dataset to confirm both `raw_*` and `stage_*` tables are emptied while `Reset Stage` still affects only `stage_*`.
